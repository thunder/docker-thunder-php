version: '3.7'

services:
  sut:
    build:
      context: test
      dockerfile: Dockerfile
  dbhost:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_RANDOM_ROOT_PASSWORD: "true"
      MYSQL_DATABASE: "thunder"
      MYSQL_USER: "thunder"
      MYSQL_PASSWORD: "thunder"
  thunder-php:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DB_HOST: "dbhost"
